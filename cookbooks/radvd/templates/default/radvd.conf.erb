interface <%= node["radvd"]["interface"] %> {
   AdvSendAdvert on;
   AdvOtherConfigFlag on;
   prefix <%= node["radvd"]["prefix"] %> {
        AdvOnLink on;
        AdvAutonomous on;
        AdvValidLifetime 2592000;
        AdvPreferredLifetime 604800;
   };
};
